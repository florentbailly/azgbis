# Dépendances du worker de rapports = celles du backend (garder synchronisé avec
# backend/requirements.txt : le code monté est le même) + rendu PDF et cartes.
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
shapely>=2.0
pyproj>=3.6
asyncpg>=0.30
truststore>=0.10
openpyxl>=3.1
# Rapports (spec §8)
jinja2>=3.1
weasyprint>=62
playwright>=1.45
